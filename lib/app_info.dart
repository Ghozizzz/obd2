/// App-wide constants surfaced in the UI.
///
/// [kAppVersion] is shown in the bottom-left of the home screen. Keep it in step
/// with the `version:` field in pubspec.yaml.
const String kAppVersion = '1.0.0';
